# WIP.md — Music Credits Scraper

> Idées ou Fonctionnalités à implémenter, Bugs ou Logique à revoir.
> Les problèmes détecté lors des sessions précédentes y sont inscrits pour implementation/correction future.
> Ce qui est corrigé ou implémenter est annoté et déplacé dans "Résolus"
> Voir `CLAUDE.md` pour l'architecture et les conventions.
> Voir `JOURNAL.md` pour l'historique détaillé, les décisions et les pièges datés.
> Voir `docs/api/` pour la Docs de référence des APIs.

---

## Certifications
Ajouter une recherche Artiste BRMA

## Données Additionnelles
- Mieux comprendre les données BPM récupérés pour déterminer si une source renvoi du half-tempo systématiquement

## Crédits & Paroles
- Liste des morceaux non matché Timestamp

## Streams
- https://kworb.net/spotify/artist/0OQMxt6l70FM2YlLwYDiEn.html non trouvé

## Export
- Rework de `export_to_json` / `to_dict` pour la génération de scripts Illustrator (décision du 2026-07-11 : pas de golden master ni de tests approfondis dessus d'ici là — seule la non-régression du crash P0 est figée).

## Refonte Phase F (COMPLÈTE 2026-07-18 — F1→F5 en une journée)
- **Différé F5 — providers lyrics/streams `fetch()` complets** : `scraping.py` (lyrics) et `streams.py` tournent désormais sous la boucle unique (`run_worker`) mais gardent leur corps sync verbatim (clients LRCLIB/YTM/Musixmatch/Kworb en `requests`/ytmusicapi). Les transformer en providers `fetch()` async (Capability LYRICS/STREAMS consommée par l'orchestrateur) = restructuration hors « comportement constant » → chantier dédié ultérieur.
- **`start_worker` conservé** (primitive daemon documentée) pour le fond purement sync sans lien avec la boucle : export SVG (`export_studio`), état sources, recherche/sélection d'artiste, saisie manuelle, dialog certifs (lance des sous-processus `update_riaa.py`/`update_brma.py`). Ne PAS les basculer en `run_worker` sans raison — ils ne touchent pas la boucle.
- ~~Vérif GUI live F2+F3~~ **FAITE 2026-07-18** (run réel : 5 morceaux Django enrichis via la boucle asyncio — httpx sur les 3 APIs, SongBPM natif async avec LLM to_thread, fermetures de fin de batch dans l'ordre prévu, saves OK ; workers non migrés intacts). Reste : le scraper Spotify async n'a pas été exercé en GUI (ISRC a tout court-circuité — comportement voulu ; vérifié hors GUI sur site réel). ~~Test de fermeture pendant un run~~ **FAIT 2026-07-18** : fermeture en plein scrape SongBPM async → task annulée, boucle abandonnée après le budget de 8 s (thread daemon, comme les anciens workers), reliquat fermé proprement, app arrêtée nickel.
- **Cosmétique (préexistant)** : gate GetSongBPM logge `raison: ` vide quand l'ISRC a déjà tout posé ; le log `⏭️ Scraper Spotify ID non nécessaire (ID déjà présent et valide: None)` est trompeur sur la voie ISRC (c'est `isrc_satisfied` qui skippe, pas un ID présent).
- **F3d (BPM Finder async) différé** : reste sur le pont sync F2 (`SerialWorker`) — fonctionnel sur la voie async, affinité de thread garantie. Migration native = vérification site réel avec le compte (login/session/quota) → à faire avec l'utilisateur, ou assumer le pont en soupape permanente (esprit F4).
- **Drift Spotify (2ᵉ, mineur)** : le TITRE de la page track est devenu générique (« Spotify – Web Player ») → `spotify_page_title` ne porte plus « Titre • Artiste » (les deux voies, comportement identique). La vérification visuelle par titre de page est aveugle tant que ça dure ; réévaluer si Spotify re-sert un vrai titre.
- **UAs custom perdus sur la voie async** : les jumeaux `*_async` passent par l'AsyncClient partagé (UA httpx par défaut) au lieu des User-Agent par client (`GetSongBPM-Python-Client/2.0`, `ReccoBeats-Python-Client/3.0`). Sans effet observé ; si une API se met à filtrer, passer `headers=` par requête dans les jumeaux.
- **Scraper Spotify non pré-créé** : l'ancien `enrich` ReccoBeats appelait le getter du scraper emprunté même quand l'ID existait déjà (création paresseuse inutile) ; les deux voies ne le créent plus que si le fallback scrape est atteint. Amélioration alignée sync/async, à garder.

## Différés Refonte E7 (post-implémentation 2026-07-15)
- **Perf LRC en observations** : `get_artist_tracks` charge les LRC (~3-10 Ko × sources × morceaux) via `_observations_by_artist` en 1 requête. Si latence perceptible au chargement d'un gros artiste → exclure les champs lourds (`lyrics_synced`) du chargement liste, réconcilier à la demande. À MESURER d'abord (témoin GUI E7d).
- **Providers lyrics/streams complets → F5** : `scraping.py` (lyrics) et `streams.py` émettent des observations depuis leurs workers ; l'extraction en providers `fetch()` complets (Capability LYRICS/STREAMS consommée par l'orchestrateur) est différée à F5 (le passage async restructure ces workers — règle « jamais structure + comportement ensemble »).
- **Retrait mutation provider audio** : `providers/songbpm.py` pose encore `track.mode="minor"` (reconcile l'écrase) ; le retrait complet des mutations `track.bpm/key/mode` = Chantier 3 E7-D1, différé post-F (recoupe F2).
- **Chantier 3 (drop colonnes audio)** : arrêt écriture legacy + backfill `source='legacy'` + drop des 9 colonnes audio. Prérequis go/no-go : `check_observations.py` en mode couverture (100 % des morceaux audio ont des observations équivalentes). Différé post-F.

## Divers
- Cas de groupe/artiste individuel
- Mettre les fenetres CDP en arriere-plan
- Lié l'etat des sources avec les echecs lors des enrichissements
- Mutualisé les recherches via le meme scraper et évité ouverture/recherche/fermeture/ouverture/recherche etc
- Intégrer les name-tag des producteurs https://genius.com/Genius-france-liners-name-tags-des-beatmakers-de-rap-francophone-lyrics
- Intégrer les origines de pseudo https://genius.com/Genius-france-explications-des-noms-de-rappeurs-annotated


### En Suspend
- **Différé (désambiguïsation YTM)** : purge rétroactive des monthly listeners écrits sur un mauvais canal (historique `monthly_listeners_history` — le gate protège l'avenir, pas le passé). *À ce jour rien à purger : les 4 artistes ont des valeurs saines (aucun homonyme). Et non automatisable proprement : la table ne stocke pas le `channel_id` d'origine → impossible de savoir offline quelle entrée était fautive. Correctif si besoin = ciblé/manuel (corriger `@handle` + `DELETE` de l'entrée).* Les seuils du gate sont désormais **dans `Settings`** (`ytm_identity_min_matched`/`ytm_identity_min_ratio`, cf. Résolus).


### Résolus
- ~~Chantier « Media » : photos d'artistes, covers (albums/singles/samples), vignettes YT + vues des clips/différenciation clip-morceau~~ **RÉSOLU 2026-07-18** (série Media 1/6→6/6) : `IMAGES_DIR` + sous-dossiers (`config.py`, gitignore `data/images/*`) ; `image_downloader` (slug Windows-safe, download atomique, nommage déterministe) + `youtube_utils` (regex video id factorisée, `thumbnail_urls`, `classify_video_kind`) ; révision Alembic **e9** (`artists.image_path` ; `tracks.cover_path`/`yt_thumbnail_path`/`youtube_video_kind`/`youtube_video_views`/`youtube_video_views_updated`) + schema/mapper/repositories (`update_track_video_views` CURRENT_TIMESTAMP, `set_artist_image_path`) ; extraction sources (Deezer `search_artist`/`get_album` + `cover_xl`/`picture_xl` ; Genius `get_artist_image`, `song_art_image_url`/`cover_art_url` transitoires) ; **`media_enricher.apply_images`** (pattern « dual certifs » : mute, ne save pas ; idempotent ; 6 catégories) + `TrackClassifier.is_show_performance` (Grünt/COLORS/Planète Rap… → EXOTIC) ; `update_video_views` (batch `fetch_video_meta_batch`, séparé de `ytm_streams`) câblé au worker Nb Streams + affichage `track_details` ; câblage GUI auto (checkbox « Télécharger les images » du dialog `get_tracks`, hook worker Discographie) + manuel (bouton « Télécharger les images… » d'Export studio). ~120 tests dédiés.
- ~~Seuils du gate d'identité YTM en dur dans le module~~ **RÉSOLU 2026-07-15** : `ytm_identity_min_matched` (2) / `ytm_identity_min_ratio` (0.3) déplacés dans `Settings` pydantic (`config.py`, réglables par env/`.env` comme les autres seuils) ; `update_ytmusic` dérive `_IDENTITY_MIN_MATCHED`/`_IDENTITY_MIN_RATIO` de la config (alias local conservé pour `_identity_suspect` + monkeypatch) ; 2 tests config + noms de compat.
- ~~Code mort `src/youtube/channel_detector.py` + `src/youtube/verification_ui.py`~~ **RÉSOLU 2026-07-15** : `verification_ui.py` importé nulle part ; `ChannelDetector` seulement ré-exporté par `src/youtube/__init__.py` (aucun call-site). Les 2 modules supprimés + import/`__all__` retirés de `__init__.py` (682 tests verts).
- ~~Durcissement désambiguïsation YTMusic (4 faiblesses après le fix du vote : pas de validation d'identité aval, départage arbitraire des homonymes, vote figé dans la colonne du pin manuel, monthly listeners écrits avant validation)~~ **RÉSOLU 2026-07-15** (5 commits E8) : (1) colonne `artists.ytm_channel_source` (`'manual'`/`'inferred'`, révision Alembic e8 + backfill `'manual'` — vérifié sur copie DB réelle : Isha/Django → `manual`) distingue saisie GUI (jamais bloquée/écrasée) et vote (ré-effaçable) ; (2) **gate d'identité** dans `update_ytmusic.update_ytmusic_streams` : confronte titres+albums du canal à la base AVANT toute écriture (`_channel_identity_report`/`_identity_suspect`, ratio CÔTÉ YTM robuste aux bases plus complètes, repêchage par album commun, seuils 2/0.3) — canal manuel divergent → warning + écriture maintenue, inféré/recherché suspect → abort SANS écriture + dé-épinglage si pin inféré ; pin inféré persisté APRÈS validation seulement ; `update_artist_monthly_listeners` déplacé après le gate ; (3) **départage homonymes** `_pick_best_candidate` par recouvrement d'albums (pluralité nette, fallback statu quo protégé par le gate) ; (4) verdict remonté au résumé GUI « Nb Streams » (🚨 aborted / ⚠️ warning). Fonctions pures testables offline + intégration à stubs : `tests/test_ytmusic_channel_inference.py`, `test_ytmusic_identity_gate.py`, `test_ytmusic_candidate_pick.py`. Non-régressions Isha (vote)/Django (manuel)/MEILLEUR (`ambiguous`) préservées.
- ~~Vote de canal YTMusic : le bon canal à 3 voix jeté comme « non concluant », fallback-par-nom prenait le mauvais canal (artiste niche+homonyme)~~ **RÉSOLU 2026-07-15** : `infer_channel_from_videos` (`src/api/ytmusic_api.py`) exigeait une MAJORITÉ ABSOLUE (`count > total/2`) ; sur un artiste dont les morceaux comptent beaucoup de featurings, les voix se dispersent → le bon canal (ex. 3/8) était rejeté puis `update_ytmusic._infer_channel_from_youtube_links` retombait sur `get_artist_channel_candidates` (recherche par nom, choisit le mauvais homonyme). Fix : règle passée à la **PLURALITÉ NETTE** (≥ 2 voix ET strictement devant le 2ᵉ ; l'égalité en tête reste rejetée car l'ordre y est arbitraire — les videoIds votants sont déjà filtrés à confiance ≥ 0.8). Décision de vote extraite en fonction pure `_channel_from_votes(votes, names)` (testable sans réseau) + 7 tests (`tests/test_ytmusic_channel_vote.py`).
- ~~Discogs : `enrich_track_data` renvoie `False` sur « rien de nouveau posé » alors que la release a matché → log « ❌ discogs ÉCHEC » trompeur + `False` comptant dans `all_failed`~~ **RÉSOLU 2026-07-15** : `enrich_track_data` (`src/api/discogs_api.py`) renvoie désormais le sentinel `"not_needed"` (déjà géré partout en aval : `_status_char` → « - », exclu de `attempted`/`all_failed` dans `_clear_after_total_failure`, ni SUCCÈS ni ÉCHEC dans `_run_step`) quand la release matche mais que rien de NOUVEAU n'est posé ; `False` réservé au vrai non-match. Provider (`enrich`) et scraping worker transmettent le sentinel (truthy → compté « success » côté phase scraping). Type élargi à `bool | str` + 5 tests (`test_enrichment_discogs.py`).
- ~~Cloudflare → fenêtre VISIBLE à chaque page alors que le captcha est résolu~~ **RÉSOLU 2026-07-15** : faux positif de `_looks_blocked` (`crawl4ai_scraper_base.py`) — il testait les sous-chaînes nues `challenge-platform`/`cf-chl`, présentes dans le beacon `/cdn-cgi/challenge-platform/…` que Cloudflare injecte sur les pages DÉJÀ PASSÉES (visible dans la fixture `tests/fixtures/genius/song_page.html`). L'essai headless réussissait mais était classé « bloqué » → fenêtre VISIBLE + warning à chaque page. Fix : (1) marqueurs resserrés à l'interstitiel réel (`just a moment`, `checking your browser`, `_cf_chl_opt`, `id="challenge-form"`…) ; (2) `blocked = (not content_found) and _looks_blocked(...)` — si le conteneur paroles a chargé, jamais bloqué. Vérifié en run réel (Django : 3 morceaux scrapés en headless, aucune fenêtre).
- ~~music_credits_scraper/data/certifications/update_log.txt mauvais dossier~~ **RÉSOLU 2026-07-15** : `RIAADatabaseUpdater` avait `base_dir="music_credits_scraper"` (nom de dossier relatif) → dossier dupliqué `music_credits_scraper/music_credits_scraper/data/…` pour le seul `update_log.txt`. Défaut passé à `None` (racine via `__file__`), dossier dupliqué purgé.
- ~~**Nettoyage certification_manager**~~ **FAIT 2026-07-15** : `certification_manager.py` supprimé (créait `unified_certifications.db` vestigiale + détournait `logging.basicConfig`), instanciation retirée de main_window, param `cert_manager` mort retiré du dialog, DB vestigiale purgée. `init_certifications.py` déjà absent.
- ~~Bug mapping Key/Mode SongBPM (`mode="minor"` vs 0/1 hétérogène en base)~~ **RÉSOLU 2026-07-15 (Refonte E5c-2b-ii)** : key/mode passent par le moteur de réconciliation (`apply_resolutions`) qui les écrit NORMALISÉS (pitch class / 0-1) dans la colonne legacy. La colonne stocke désormais toujours `0`/`1` (vérifié : Malavida `mode='0'`, Migraine `mode='1'`). NB : `providers/songbpm.py` pose encore la chaîne provisoire `track.mode="minor"`, mais reconcile l'écrase — retrait de la mutation provider = étape « suppression complète du ballot » (différée).