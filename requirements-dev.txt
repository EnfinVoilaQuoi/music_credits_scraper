# Outils de développement (pas nécessaires à l'exécution de l'app).
# Versions épinglées pour que le formatage/linting soit reproductible.
black==26.5.1
ruff==0.15.21
pytest==9.1.1
pytest-cov==7.1.0

# Debug visuel du pipeline Bubble Prod (CLI `scripts/bubble_prod.py --debug`,
# import lazy uniquement — jamais importé par l'app ni les tests).
matplotlib>=3.8
