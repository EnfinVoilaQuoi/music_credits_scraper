# APIs
lyricsgenius==3.0.1
discogs-client==2.3.0
pylast==5.2.0

# Scraping
selenium>=4.15.2
playwright>=1.40.0
beautifulsoup4>=4.12.2
requests>=2.31.0

# Interface graphique
customtkinter==5.2.1

# Base de données (migration SQLAlchemy Core + Alembic, phase E).
# NB : 2.0.23 était incompatible Python 3.13 (import échoue : garde TypingOnly
# vs __firstlineno__/__static_attributes__ ajoutés en 3.13, corrigé en 2.0.31+).
sqlalchemy==2.0.51
alembic==1.18.5

# Configuration (typée/validée + .env natif)
pydantic>=2.7
pydantic-settings>=2.0

# Utilitaires
python-dotenv==1.0.0
coloredlogs==15.0.1
pandas>=2.0.0
numpy>=1.24.3

# Dataviz / export SVG (Bubble Prod) — graphe de collaboration + layout + SVG.
# networkx était présent en transitif seulement ; rendu direct ici. Pas de scipy
# (spring_layout numpy-only à l'échelle d'un album). numpy déjà listé ci-dessus.
networkx>=3.3
svgwrite>=1.4.3

# Chrome driver
webdriver-manager==4.0.1

# YouTube integration
ytmusicapi==1.8.0
google-api-python-client>=2.100.0

# Planification de tâches
schedule

# Logging (optionnel, mais utile)
python-dateutil==2.8.2

# IA locale (Crawl4AI + Ollama)
crawl4ai>=0.5.0
ollama>=0.4.0

# Concurrence async (phase F) — client HTTP des providers async (boucle unique).
# Déjà présent en transitif (crawl4ai, ollama) ; dépendance directe désormais.
httpx>=0.28.1